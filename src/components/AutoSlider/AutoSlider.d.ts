import { Posts } from "../landingPage/LandingPage";
interface AutoSliderProps {
    posts: Posts;
}
declare const AutoSlider: ({ posts }: AutoSliderProps) => import("react/jsx-runtime").JSX.Element;
export default AutoSlider;
